# LEAN dependency licensing

LEAN is not vendored into Forakilo. The external checkout pinned by
`LEAN.lock.json` is licensed under Apache-2.0. Its transitive dependencies and
the separately sourced OANDA Practice brokerage plugin require verification
before any adapter runtime is approved.

